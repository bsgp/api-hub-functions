module.exports = async (draft, { request }) => {
  // your script
  // check s3Key 0115
  // why didn`t
  // come on!!!
  // i can do it
  // one more!!
  // lookUp man!
  console.log(draft, request);
};
